name: Publish Pages

on:
    workflow_run:
        workflows: ['Build the project', 'Playwright Tests']
        types: [completed]
        branches: [master]

permissions:
    contents: read
    pages: write
    id-token: write

jobs:
    publish:
        runs-on: ubuntu-latest
        #  && github.event_name == 'push' && github.ref == 'refs/heads/master'
        if: ${{ github.event.workflow_run.conclusion == 'success' && needs.Build_the_project.result == 'success' && needs.Playwright_Tests.result == 'success' }}
        timeout-minutes: 5
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}

        steps:
            - name: Git Checkout
              uses: actions/checkout@v3

            - name: Setup Node
              id: setup_node_step
              uses: actions/setup-node@v3
              with:
                  node-version: '20'

            - name: Enable Cache
              id: cache_step
              uses: actions/cache@v3
              with:
                  key: ${{ hashFiles('yarn.lock') }}
                  path: node_modules

            - if: ${{ steps.cache_step.outputs.cache-hit != 'true' }}
              run: yarn install --frozen-lockfile

            - name: build
              run: yarn build

            - name: Setup Pages
              uses: actions/configure-pages@v3

            - name: Upload Pages Artifact
              uses: actions/upload-artifact@v2
              with:
                  name: github-pages
                  path: docs/ # Измените на директорию, где находятся собранные файлы

            - name: Deploy Pages
              id: deployment
              uses: actions/deploy-pages@v2

    empty-job:
        runs-on: ubuntu-latest
        steps:
            - name: Do nothing
              run: |
                  echo ${{ github.event_name }}
                  echo ${{ github.ref }}
                  echo ${{ github.event.workflow_run.conclusion }}
                  echo ${{ workflows.Build_the_project.result }}
                  echo ${{ workflows.Playwright_Tests.result }}
